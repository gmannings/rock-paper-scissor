/**
 * Enum like data structure to pass arguments.
 *
 * @type {{LOSS: string, DRAW: string, WIN: string}}
 */
export const GameOutcome = Object.seal({
    WIN: 'win',
    LOSS: 'loss',
    DRAW: 'draw'
});